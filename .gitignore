Gemini_Generated_Image_*.png
.venv-transparency/

# Per-PoC secrets / build output
# Note: portals/*/config.js holds only publishable/anon keys (safe in browsers).
# Real secrets live in .env / .dev.vars / Pages env vars — never in config.js.
portals/*/.env
portals/*/.env.local
portals/*/node_modules/
portals/*/.next/
portals/*/.vercel/
portals/*/.wrangler/
portals/*/dist/
portals/*/.dev.vars
