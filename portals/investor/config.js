/* Local config for PoC 01 (Supabase). Gitignored — see ../../.gitignore.
   Project URL is pre-filled from your dashboard link. You still need to paste
   the anon key from Supabase → Project Settings → API → "Project API keys"
   → "anon / public". The anon key is safe to expose to browsers — data
   security comes from the RLS policies in schema.sql, not key secrecy. */

export const SUPABASE_URL = 'https://aitzjjxsizvzglypabwu.supabase.co';
export const SUPABASE_ANON_KEY = 'sb_publishable_Tp92GEXEDCgMkp-U_IJMEg_qlXQ7eR_';
