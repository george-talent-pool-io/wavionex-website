/* Copy this file to `config.js` and fill in the values from your Supabase
   project (Project Settings → API). The anon key is safe to expose to the
   browser; data security comes from Row-Level Security on the tables, not
   key secrecy. Never paste the `service_role` key here. */

export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-PUBLIC-ANON-KEY';
